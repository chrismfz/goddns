/var/log/goddns.log /var/log/goddns-access.log {
    weekly
    rotate 8
    compress
    delaycompress
    missingok
    notifempty
    # goddns keeps the file handles open; copytruncate avoids any reopen
    # signal and the file inodes (and goddns:goddns ownership) survive.
    copytruncate
}
